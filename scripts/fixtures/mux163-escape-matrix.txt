# MUX-163 Phase 1 — live escape-adjacency matrix on a real Claude Code composer.
# Generated by scripts/probe-escape-matrix.sh; re-running overwrites this file.
# Records, per shape, whether a payload's first character survived after an Escape,
# and (window rows) the gap band that separates the LOST band from the KEPT band.
# claude 2.1.258 (Claude Code) · tmux tmux 3.6a · model haiku · 2026-09-09 12:31
a  Escape → text: first-char LOST  composer='ello world'
a2 Escape → dash text: first-char KEPT  composer='- dash probe'
a3 Escape → one char: first-char LOST  composer=''
b  Escape → 100ms → text: first-char KEPT  composer='hello world'
c  Escape → 100ms → C-e → 100ms → text: first-char KEPT  composer='hello world'
gap Escape → 0.2s → text: first-char KEPT  composer='hello world'
gap Escape → 0.3s → text: first-char KEPT  composer='hello world'
gap Escape → 0.4s → text: first-char KEPT  composer='hello world'
gap Escape → 0.5s → text: first-char KEPT  composer='hello world'
gap Escape → 0.6s → text: first-char KEPT  composer='hello world'
gap Escape → 0.8s → text: first-char KEPT  composer='hello world'
gap Escape → 1.0s → text: first-char KEPT  composer='hello world'
window Escape → 0s → text: first-char kept 0/5
window Escape → 0.01s → text: first-char kept 0/5
window Escape → 0.02s → text: first-char kept 0/5
window Escape → 0.03s → text: first-char kept 0/5
window Escape → 0.05s → text: first-char kept 5/5
window Escape → 0.075s → text: first-char kept 5/5
d  parked → Escape → 50ms → Enter: SUBMITTED
e  parked → Escape → 100ms → C-e → 100ms → Enter: SUBMITTED
